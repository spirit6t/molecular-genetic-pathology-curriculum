rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Images folder - allow read/write for all users (for development)
    match /images/{imageId} {
      allow read: if true;
      allow write: if true;
    }
  }
}

